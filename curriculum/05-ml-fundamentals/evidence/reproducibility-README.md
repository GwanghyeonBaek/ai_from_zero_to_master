# Reproducibility README
## Commands
- python -m venv .venv && source .venv/bin/activate
- pip install -r requirements.txt
- python curriculum/05-ml-fundamentals/solutions/src/pipeline.py
## Policy
- Seed default: 42
- Data path convention: data/*.csv
- Log package versions and commit SHA in every experiment report.
