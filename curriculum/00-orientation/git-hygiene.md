# Git Hygiene
Date (UTC): 2026-02-21

## Global Config
```bash
git config --global user.name "Learner"
git config --global user.email "learner@example.com"
git config --global init.defaultBranch main
git config --list --show-origin
```

## Atomic Commit Policy
- One intent per commit.
- Commit message format: `type(scope): summary`.
- Minimum three commits during orientation.

## Remote Push Proof (redacted)
- Commit hash pushed: `7a27d07`
- Remote URL (redacted): `git@github.com:***/*ai-data-from-zero-to-pro.git`
- Push timestamp (UTC): `2026-02-21 13:17:54 UTC`
